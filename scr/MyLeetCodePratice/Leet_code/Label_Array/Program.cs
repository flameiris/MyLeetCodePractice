using System;

namespace Label_Array
{
    class Program
    {
        static void Main(string[] args)
        {

        }


        #region 两数之和
        ////static void Main(string[] args)
        ////{
        ////    //给定一个整数数组 nums和一个目标值 target，请你在该数组中找出和为目标值的那 两个整数，并返回他们的数组下标。

        ////    //你可以假设每种输入只会对应一个答案。但是，你不能重复利用这个数组中同样的元素。

        ////    //示例:

        ////    //给定 nums = [2, 7, 11, 15], target = 9

        ////    //因为 nums[0] +nums[1] = 2 + 7 = 9
        ////    //所以返回[0, 1]

        ////    var s = new Solution();

        ////    var arr = new int[] { 2, 7, 11, 15 };

        ////    var res1 = s.TwoSum1(arr, 26);

        ////    var res2 = s.TwoSum2(arr, 26);

        ////    var res3 = s.TwoSum3(arr, 26);


        ////    Console.ReadKey();
        ////}
        #endregion


        #region 删除排序数组中的重复项

        //static void Main(string[] args)
        //{
        //    //给定一个 【排序数组】，你需要在原地删除重复出现的元素，使得每个元素只出现一次，返回移除后数组的新长度。
        //    //不要使用额外的数组空间，你必须在原地修改输入数组并在使用 O(1) 额外空间的条件下完成。

        //    //示例一：
        //    //给定数组 nums = [1, 1, 2], 【数组已经排过序了~~~】
        //    //函数应该返回新的长度 2, 并且原数组 nums 的前两个元素被修改为 1, 2。  【长度是去重之后的个数】
        //    //你不需要考虑数组中超出新长度后面的元素。

        //    //示例二
        //    //给定 nums = [0, 0, 1, 1, 1, 2, 2, 3, 3, 4],  【数组已经排过序了~~~】
        //    //函数应该返回新的长度 5, 并且原数组 nums 的前五个元素被修改为 0, 1, 2, 3, 4。
        //    //你不需要考虑数组中超出新长度后面的元素。



        //    Console.ReadKey();
        //}

        #endregion

    }
}
